"""Development tooling for maintaining the proxmox-vm-sdk codebase.

These are maintainer utilities, not part of the supported public API.
``code_eval`` scans the source tree for code smells, ``package_report``
summarises the import graph, and ``quality`` runs the lint and type-check
gates.
"""
